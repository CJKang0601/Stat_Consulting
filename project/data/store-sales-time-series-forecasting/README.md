The original Kaggle train.csv is intentionally excluded from Git tracking because
its file size exceeds GitHub's single-file upload limit.
Use the archive in project/archives or re-download the dataset if full retraining is needed.
